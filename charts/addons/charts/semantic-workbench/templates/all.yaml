{{- if .Values.persistence.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "sw.fullname" . }}-data
  labels: {{- include "sw.labels" . | nindent 4 }}
spec:
  accessModes: [{{ .Values.persistence.accessMode | quote }}]
  {{- if .Values.persistence.storageClass }}
  storageClassName: {{ .Values.persistence.storageClass | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
---
{{- end }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "sw.fullname" . }}
  labels: {{- include "sw.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector: {{- include "sw.selectorLabels" . | nindent 4 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "sw.fullname" . }}
  labels: {{- include "sw.labels" . | nindent 4 }}
spec:
  replicas: 1
  strategy: {type: Recreate}
  selector:
    matchLabels: {{- include "sw.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels: {{- include "sw.labels" . | nindent 8 }}
    spec:
      containers:
        - name: semantic-workbench
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: KEYCLOAK_URL
              value: {{ required "keycloak.url must be set" .Values.keycloak.url | quote }}
            - name: KEYCLOAK_REALM
              value: {{ .Values.keycloak.realm | quote }}
            - name: POOLPARTY_URL
              value: {{ .Values.poolparty.internalUrl | quote }}
            - name: SPRING_SECURITY_OAUTH2_CLIENT_REGISTRATION_PPT_CLIENTSECRET
              value: {{ .Values.keycloak.clientSecret | quote }}
            - name: POOLPARTY_LICENSE_PATH
              value: /usr/share/semantic-workbench
            - name: MANAGEMENT_ENDPOINTS_WEB_CORS_ALLOWED_ORIGINS
              value: {{ .Values.externalUrl | quote }}
            - name: APPLICATION_VERSION
              value: {{ .Values.applicationVersion | quote }}
          ports:
            - name: http
              containerPort: 8080
          resources: {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/semantic-workbench
            - name: license
              mountPath: /usr/share/semantic-workbench/poolparty.key
              subPath: {{ .Values.license.secretKey }}
              readOnly: true
      volumes:
        - name: license
          secret:
            secretName: {{ .Values.license.secretName }}
        - name: data
          {{- if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "sw.fullname" . }}-data
          {{- else }}
          emptyDir: {}
          {{- end }}
{{- if .Values.ingress.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "sw.fullname" . }}
  labels: {{- include "sw.labels" . | nindent 4 }}
  annotations:
spec:
  ingressClassName: {{ .Values.ingress.className }}
  tls:
    - hosts: [{{ include "sw.host" . | quote }}]
      secretName: wildcard-tls
  rules:
    - host: {{ include "sw.host" . | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "sw.fullname" . }}
                port:
                  number: {{ .Values.service.port }}
{{- end }}
