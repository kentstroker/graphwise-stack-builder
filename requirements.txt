# Python dependencies installed on the EC2 by user-data.sh.tpl
# (`pip3 install --ignore-installed -r requirements.txt`).

# ── Used directly by the tracked stack scripts ──────────────────────────────
PyYAML>=6.0                # values/secrets YAML splicing (render-values, reset-helm, config scripts)
boto3>=1.34                # AWS SDK (Bedrock creds, S3)
requests-aws4auth>=1.3     # SigV4-signed AWS requests
opensearch-py>=2.4         # OpenSearch client
elasticsearch>=8.0         # Elasticsearch client

# ── Ingest / RAG document-processing (used by the out-of-band n8n-workflows
#    ingest pipeline run on this host; keep if you run those demos) ───────────
requests>=2.31
beautifulsoup4>=4.12
python-docx>=1.1
rdflib>=7.0
openpyxl>=3.1
python-dotenv>=1.0
pdfplumber>=0.11
tiktoken>=0.7
langchain-text-splitters>=0.2
pytest>=8.0
