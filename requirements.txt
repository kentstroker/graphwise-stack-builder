# Python dependencies installed on the deployment host by user-data.sh.tpl
# (`pip3 install --ignore-installed -r requirements.txt`).
#
# Scope: what the tracked stack scripts under scripts/ need.
# The document-processing dependencies for the out-of-band ingest workflows
# live in requirements-ingest.txt and are NOT installed by default.

PyYAML>=6.0                # values/secrets YAML splicing (render-values.sh, reset-helm.sh)
boto3>=1.34                # AWS SDK (Bedrock credentials, S3)
requests-aws4auth>=1.3     # SigV4-signed AWS requests
opensearch-py>=2.4         # OpenSearch client
elasticsearch>=8.0         # Elasticsearch client
requests>=2.31             # HTTP calls in helper scripts
